#!/bin/bash
# popp - Pop with minimal mode enabled by default
# This is a convenience wrapper that calls pop with --minimal flag

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Call pop with --minimal flag, passing through all arguments
exec "$SCRIPT_DIR/pop" --minimal "$@"
